<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    ServerName aslsquads.com
    ServerAlias www.aslsquads.com
    DocumentRoot /var/www/html/aslsquads.com
    ErrorLog ${APACHE_LOG_DIR}/aslsquads_error.log
    CustomLog ${APACHE_LOG_DIR}/aslsquads_access.log combined

    # Force HTTP to HTTPS
    RewriteEngine On
    RewriteCond %{SERVER_NAME} =aslsquads.com [OR]
    RewriteCond %{SERVER_NAME} =www.aslsquads.com
    RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]

    # Proxy for Socket.IO WebSocket connections
    ProxyPass /socket.io http://localhost:4000/socket.io
    ProxyPassReverse /socket.io http://localhost:4000/socket.io

    # Proxy for GraphQL
    ProxyPass /graphql http://localhost:4000/graphql
    ProxyPassReverse /graphql http://localhost:4000/graphql

    # Proxy for WebSocket
    ProxyPass /websocket ws://localhost:4000/websocket
    ProxyPassReverse /websocket ws://localhost:4000/websocket

    # Proxy for main app
    ProxyPass / http://localhost:3001/
    ProxyPassReverse / http://localhost:3001/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    ServerName admin.aslsquads.com
    DocumentRoot /var/www/html
    ErrorLog ${APACHE_LOG_DIR}/admin_aslsquads_error.log
    CustomLog ${APACHE_LOG_DIR}/admin_aslsquads_access.log combined

    # Force HTTP to HTTPS for admin.aslsquads.com
    RewriteEngine On
    RewriteCond %{SERVER_NAME} =admin.aslsquads.com
    RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]

    # Proxy for Socket.IO WebSocket connections
    ProxyPass /socket.io http://localhost:3000/socket.io
    ProxyPassReverse /socket.io http://localhost:3000/socket.io

    # Proxy for admin site
    ProxyPass / http://localhost:3000/
    ProxyPassReverse / http://localhost:3000/
</VirtualHost>
