services:
  ys_frontend:
    build: .
    container_name: ys_frontend_app
    env_file:
      - .env
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
